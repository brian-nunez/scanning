# Airflow Worker

The `airflow-worker` is a Go service that acts as the **second stage** of the scanning pipeline.  
It consumes job messages from Redis Streams, triggers Apache Airflow DAG runs, polls for completion, fetches results, and writes the final status back to Postgres.

---

## Table of Contents

1. [Architecture Overview](#1-architecture-overview)
2. [Data Model](#2-data-model)
3. [Configuration Reference](#3-configuration-reference)
4. [Startup & Shutdown Lifecycle](#4-startup--shutdown-lifecycle)
5. [Stream Priority & Burst Scheduling](#5-stream-priority--burst-scheduling)
6. [Main Processing Flow](#6-main-processing-flow)
7. [Airflow API Integration](#7-airflow-api-integration)
8. [Retry & Max-Attempts Logic](#8-retry--max-attempts-logic)
9. [Acknowledgement (`XACK`)](#9-acknowledgement-xack)
10. [Error Scenarios](#10-error-scenarios)

---

## 1. Architecture Overview

```
┌──────────────────────────────────────────────────────────────────────┐
│                          airflow-worker                              │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  Controller goroutine (Start)                               │    │
│  │    ├─ ensureConsumerGroup (scan_jobs:urgent)                │    │
│  │    ├─ ensureConsumerGroup (scan_jobs:normal)                │    │
│  │    └─ spawn N × localWorkerLoop goroutines                  │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────┐  ┌─────────────┐        ┌─────────────┐           │
│  │  worker #1  │  │  worker #2  │  ···   │  worker #N  │           │
│  │  loop       │  │  loop       │        │  loop       │           │
│  └──────┬──────┘  └──────┬──────┘        └──────┬──────┘           │
└─────────┼────────────────┼────────────────────── ┼──────────────────┘
          │                │                        │
    ┌─────▼────────────────▼────────────────────────▼─────┐
    │               Redis Streams                          │
    │   scan_jobs:urgent     scan_jobs:normal              │
    └──────────────────────────────────────────────────────┘
          │
    ┌─────▼───────────────┐     ┌──────────────────────────┐
    │  Apache Airflow API │     │  Postgres (scanning DB)  │
    │  POST /dagRuns      │     │  recurring_scan_runs     │
    │  GET  /dagRuns/{id} │     └──────────────────────────┘
    │  GET  xcomEntries   │
    └─────────────────────┘
```

Each `localWorkerLoop` goroutine independently reads from Redis, processes one message at a time, and communicates with Airflow and Postgres.

---

## 2. Data Model

### `recurring_scan_runs` (relevant columns)

The `airflow-worker` updates this table — it does **not** insert rows (that is the scheduler's job).

| Column | Updated by airflow-worker | Value written |
|---|---|---|
| `status` | ✅ | `running` → `succeeded` / `max_attempts_exceeded` / `enqueued` (retry) |
| `attempt_count` | ✅ | Incremented by 1 each time the message is processed |
| `started_at` | ✅ | Set to `now()` on the **first** attempt (`COALESCE(started_at, now())`) |
| `finished_at` | ✅ | Set to `now()` when status becomes terminal |
| `airflow_dag_id` | ✅ | Value of `AIRFLOW_DAG_ID` config |
| `airflow_dag_run_id` | ✅ | DAG run ID returned/generated by the trigger call |
| `result` | ✅ | JSONB from Airflow XCom or fallback static payload |
| `failure_reason` | ✅ | Short label (`trigger_failed`, `monitor_failed`, `dag_failed`, `max_attempts_exceeded`) |
| `failure_details` | ✅ | Structured JSON with `reason` and `error` fields |

### Redis Streams & Consumer Groups

| Stream | Consumer group | Notes |
|---|---|---|
| `scan_jobs:urgent` | `scan-workers` (configurable) | High-priority jobs |
| `scan_jobs:normal` | `scan-workers` (configurable) | Normal-priority jobs |

The consumer group is created with `XGROUP CREATE … MKSTREAM 0` on startup. The `0` offset means all pre-existing messages in the stream will also be visible to this consumer group.

---

## 3. Configuration Reference

| Environment Variable | Default | Description |
|---|---|---|
| `POSTGRES_DSN` | `postgres://scanning:scanning@postgres:5432/scanning?sslmode=disable` | Postgres connection string |
| `REDIS_ADDR` | `redis:6379` | Redis host:port |
| `REDIS_PASSWORD` | _(empty)_ | Redis password |
| `REDIS_DB` | `0` | Redis logical database index |
| `REDIS_STREAM_NORMAL` | `scan_jobs:normal` | Normal-priority stream name |
| `REDIS_STREAM_URGENT` | `scan_jobs:urgent` | Urgent-priority stream name |
| `REDIS_CONSUMER_GROUP` | `scan-workers` | Consumer group name used for `XREADGROUP` |
| `REDIS_CONSUMER_NAME` | OS hostname | Per-consumer name used in `XREADGROUP` |
| `WORKERS_PER_CONTROLLER` | `4` | Number of concurrent `localWorkerLoop` goroutines per process |
| `REDIS_READ_COUNT` | `1` | Messages to read per `XREADGROUP` call |
| `REDIS_READ_BLOCK` | `5s` | Long-poll block duration for the fallback blocking `XREADGROUP` |
| `REDIS_URGENT_BURST` | `20` | Max consecutive urgent messages before forcing a normal-queue read |
| `AIRFLOW_BASE_URL` | `http://airflow-webserver:8080` | Airflow webserver base URL |
| `AIRFLOW_DAG_ID` | `scan_example` | DAG to trigger for every scan job |
| `AIRFLOW_TASK_ID` | `run_scan` | Task whose XCom `return_value` is fetched as the result |
| `AIRFLOW_USERNAME` | `admin` | Basic auth username for the Airflow REST API |
| `AIRFLOW_PASSWORD` | `admin` | Basic auth password for the Airflow REST API |
| `AIRFLOW_POLL_INTERVAL` | `3s` | How often to poll `GET /dagRuns/{id}` while waiting |
| `AIRFLOW_RUN_TIMEOUT` | `5m` | Deadline for a single DAG run to reach a terminal state |

**Validation errors** at startup (process exits):
- `WORKERS_PER_CONTROLLER` ≤ 0
- `REDIS_READ_COUNT` ≤ 0
- `REDIS_URGENT_BURST` ≤ 0

---

## 4. Startup & Shutdown Lifecycle

```
main()
  ├─ LoadConfigFromEnv()           validate env, fail-fast on invalid values
  ├─ worker.New()                  create Postgres pool + Redis client + http.Client
  ├─ signal.NotifyContext()        listen for SIGINT / SIGTERM
  ├─ goroutine: svc.Start(ctx)
  │     ├─ ping()                  10 s ping to Postgres and Redis
  │     ├─ ensureConsumerGroup(scan_jobs:urgent)
  │     ├─ ensureConsumerGroup(scan_jobs:normal)
  │     ├─ spawn WORKERS_PER_CONTROLLER goroutines → localWorkerLoop(ctx, id)
  │     └─ <-ctx.Done()            block until shutdown
  │           workerGroup.Wait()   wait for all goroutines to exit
  └─ on signal / Start() error:
        svc.Stop(10 s ctx)         close Postgres pool + Redis client (once, via sync.Once)
```

`sync.Once` in `Stop()` ensures resources are released exactly once even if called concurrently.  
`started` boolean + `startedMutex` in `Start()` ensures goroutines are spawned exactly once even if `Start()` is somehow called twice.

---

## 5. Stream Priority & Burst Scheduling

Each `localWorkerLoop` goroutine maintains a local `urgentProcessed` counter. The read strategy is:

```
readNextMessage(urgentProcessed):
  1. If urgentProcessed < UrgentBurst:
       try non-blocking read from scan_jobs:urgent (250 ms timeout)
       if message found → return it (urgentProcessed++)
  2. Try non-blocking read from scan_jobs:normal (250 ms timeout)
       if message found → return it (urgentProcessed = 0)
  3. Blocking read from BOTH streams simultaneously (ReadBlock timeout)
       return first available message from either stream
```

After processing each message:
- If the message came from `scan_jobs:urgent`: `urgentProcessed++`
- If the message came from `scan_jobs:normal`: `urgentProcessed = 0`
- When `urgentProcessed >= UrgentBurst`: reset to 0 (next iteration falls through to normal queue)

This means: for every burst of `UrgentBurst` (default 20) urgent messages, at least one normal-queue message is processed, preventing normal-queue starvation.

---

## 6. Main Processing Flow

`processMessage(event)` handles one Redis Stream message:

```
processMessage(event)
  │
  ├─ 1. Extract scan_run_id from message values
  │       If blank → XACK and return  (malformed message, discard)
  │
  ├─ 2. markRunRunning()
  │       UPDATE recurring_scan_runs
  │         SET status='running', attempt_count=attempt_count+1,
  │             started_at=COALESCE(started_at, now())
  │       RETURNING attempt_count, max_attempts
  │
  ├─ 3. If attempt_count > max_attempts:
  │       markMaxAttemptsExceeded() → status='max_attempts_exceeded'
  │       XACK message
  │       return   ← terminal, no more retries
  │
  ├─ 4. Build Airflow payload (see §7)
  │
  ├─ 5. triggerDAG()
  │       POST /api/v1/dags/{dag_id}/dagRuns
  │       On error → handleExecutionFailure(reason="trigger_failed")
  │
  ├─ 6. attachDagRunID()
  │       UPDATE recurring_scan_runs SET airflow_dag_id, airflow_dag_run_id
  │
  ├─ 7. waitForDAGCompletion()
  │       Poll GET /api/v1/dags/{dag_id}/dagRuns/{dag_run_id}
  │       every AirflowPollInterval until state is "success" or "failed"
  │       Timeout after AirflowRunTimeout
  │       On error → handleExecutionFailure(reason="monitor_failed")
  │
  ├─ 8. If DAG state != "success":
  │       handleExecutionFailure(reason="dag_failed")
  │
  ├─ 9. fetchDAGResult()
  │       GET /api/v1/dags/{dag_id}/dagRuns/{run_id}/taskInstances/{task_id}/xcomEntries/return_value
  │       On error → use fallback static payload (log WARN)
  │
  ├─ 10. markSucceeded()
  │        UPDATE status='succeeded', result=$jsonb, finished_at=now()
  │
  └─ 11. XACK message
```

### `handleExecutionFailure(event, scanRunID, attempt, maxAttempts, reason, err)`

```
If attempt >= maxAttempts:
  markMaxAttemptsExceeded(reason, details)  → status='max_attempts_exceeded'
  XACK message
  return

Else (retryable):
  requeueForRetry()
    UPDATE status='enqueued', failure_reason, failure_details
    XAdd to same stream (re-enqueue with same payload fields)
  XACK original message
  return
```

---

## 7. Airflow API Integration

### Trigger DAG (`POST /api/v1/dags/{dag_id}/dagRuns`)

**DAG Run ID format:**
```
scan_<scanRunIDWithoutDashes>_attempt_<attemptCount>_<unixTimestamp>
```
Example: `scan_abc123def456_attempt_1_1714000000`

**Request body:**
```json
{
  "dag_run_id": "scan_abc123..._attempt_1_1714000000",
  "conf": {
    "scan_run_id": "<uuid>",
    "redis_message_id": "<stream-message-id>",
    "recurring_scan_id": "<uuid>",
    "asset_id": "<uuid>",
    "asset_type": "url",
    "priority": "10"
  }
}
```

Authentication: HTTP Basic Auth (`AIRFLOW_USERNAME` / `AIRFLOW_PASSWORD`).  
Timeout: 30 seconds (shared `http.Client` timeout).

A non-2xx response returns the raw response body in the error message.

### Poll DAG State (`GET /api/v1/dags/{dag_id}/dagRuns/{dag_run_id}`)

Polled every `AIRFLOW_POLL_INTERVAL` (default 3 s) until state is `success` or `failed`.  
Overall deadline is `AIRFLOW_RUN_TIMEOUT` (default 5 min) from when polling started.

### Fetch XCom Result (`GET …/taskInstances/{task_id}/xcomEntries/return_value`)

The result value may be returned as:
- A JSON object — used directly.
- A JSON string — decoded as JSON; if that fails, wrapped as `{"value": "<string>"}`.
- Any other type — serialised to JSON and wrapped as `{"value": "<json>"}`.

If this call fails for any reason, the worker falls back to a static payload:
```json
{
  "status":  "ok",
  "payload": "static",
  "source":  "fallback",
  "dag_run": "<dag_run_id>"
}
```
This fallback means the run will still be marked `succeeded` even if XCom retrieval fails — preserving liveness at the cost of missing detailed results.

### Example DAG (`scan_example`)

The bundled example DAG (`airflow/dags/scan_example.py`) is a minimal stub:

```python
@task(task_id="run_scan")
def run_scan() -> dict:
    time.sleep(10)   # simulates scan work
    return {
        "status": "ok",
        "source": "scan_example",
        "payload": {"message": "static payload", "version": 1},
    }
```

- `schedule=None` — only triggered via the REST API, never by Airflow's own scheduler.
- `catchup=False` — no backfilling.
- Returns a static dict that becomes the XCom `return_value`.

---

## 8. Retry & Max-Attempts Logic

| Condition | Action |
|---|---|
| `attempt_count > max_attempts` (checked **before** trigger) | Immediately mark `max_attempts_exceeded`, XACK, discard |
| `attempt >= max_attempts` (checked **after** a failure) | Mark `max_attempts_exceeded`, XACK, discard |
| `attempt < max_attempts` (after a failure) | Re-enqueue to same stream, XACK original, status = `enqueued` |

`max_attempts` is set at run-creation time by the `scheduler-worker` (default 3). The counter is incremented to 1 on the first processing attempt, so with `max_attempts=3` the worker will try: attempt 1, 2, 3 → on failure at attempt 3 the run is marked `max_attempts_exceeded`.

When re-enqueuing for retry, the **same stream** as the original message is used (preserving original priority).

---

## 9. Acknowledgement (`XACK`)

`XACK` is called via `w.ack(ctx, stream, messageID)` only in the following terminal cases:

| Outcome | XACK called? |
|---|---|
| Message has blank `scan_run_id` (malformed) | ✅ Yes |
| `attempt_count > max_attempts` before trigger | ✅ Yes |
| DAG succeeded, result stored | ✅ Yes |
| Any failure at `attempt >= max_attempts` | ✅ Yes |
| Any failure at `attempt < max_attempts` (retried via re-enqueue) | ✅ Yes (original message ACKed, new message re-added) |
| `markRunRunning` fails (Postgres error) | ❌ No — message remains in PEL for manual recovery or consumer restart |
| `attachDagRunID` fails | ❌ No |
| `markSucceeded` fails | ❌ No |
| `requeueForRetry` XAdd fails | ❌ No |

**Important:** Un-ACKed messages remain in the consumer's Pending Entry List (PEL). They are not re-delivered automatically to the same consumer — a separate PEL recovery mechanism (e.g. `XAUTOCLAIM` or a watchdog) would be needed if the consumer crashes after triggering but before ACKing.

---

## 10. Error Scenarios

### 10.1 Postgres unreachable at startup

`ping()` returns an error → `Start()` returns → `main()` exits with code 1.

### 10.2 Redis unreachable at startup

Same as above.

### 10.3 Consumer group creation fails

`ensureConsumerGroup()` returns an error (non-`BUSYGROUP` Redis error) → `Start()` returns → `main()` exits with code 1.

### 10.4 Redis read error inside `localWorkerLoop`

Error is logged as `ERROR "failed reading redis stream"`. The goroutine sleeps 1 second and retries. If the error is `context.Canceled` (shutdown), the goroutine exits cleanly.

### 10.5 Malformed message (missing `scan_run_id`)

The message is immediately XACK'd and discarded. No Postgres update is made.

### 10.6 `markRunRunning` fails (Postgres error)

`processMessage` returns the error. The `localWorkerLoop` logs `ERROR "failed processing message"` and reads the next message. The original Redis message is **not** XACK'd — it stays in the PEL and will not be re-delivered to this consumer automatically.

### 10.7 DAG trigger fails (`trigger_failed`)

`handleExecutionFailure` is called:
- If retries remain: re-enqueued to same stream, original XACK'd, run status = `enqueued`.
- If no retries remain: run marked `max_attempts_exceeded`, XACK'd.

Common causes:
- Airflow webserver not yet healthy (startup race).
- Network error between worker and Airflow.
- Airflow returns 4xx/5xx (e.g. DAG not found, DAG paused, duplicate `dag_run_id`).

### 10.8 DAG monitoring timeout (`monitor_failed`)

`waitForDAGCompletion` exceeds `AIRFLOW_RUN_TIMEOUT`. Same retry/terminal logic as trigger failure.

Common causes:
- Airflow executor has no available slots (queue buildup).
- The DAG task hangs or deadlocks.
- Airflow API is temporarily unreachable during polling.

### 10.9 DAG run reports `failed` state (`dag_failed`)

Same retry/terminal logic. The DAG's own internal error is not surfaced beyond the `dag_failed` label in Postgres.

### 10.10 XCom fetch fails

The worker logs `WARN "failed fetching airflow xcom result; using fallback static payload"`. The run is still marked `succeeded` with the static fallback payload. This is a non-fatal degradation.

### 10.11 `markSucceeded` fails (Postgres error)

`processMessage` returns the error. XACK is **not** called. The next attempt by the same consumer will re-process the message (incrementing `attempt_count`), potentially triggering another DAG run.

### 10.12 `requeueForRetry` XAdd to Redis fails

`processMessage` returns the error. XACK is not called. The original message remains in the PEL.

### 10.13 Multiple `airflow-worker` replicas

Safe to run in parallel. Each replica has its own `REDIS_CONSUMER_NAME` (hostname by default) within the shared consumer group. Redis Streams consumer group semantics ensure each message is delivered to exactly one consumer at a time. The `attempt_count` in Postgres is incremented atomically, so even if a message is somehow processed by two consumers concurrently (e.g. after a PEL takeover), the attempt counter guards against unbounded retries.

### 10.14 Worker crashes after DAG trigger but before XACK

The message stays in the PEL indefinitely. A `XAUTOCLAIM`-based recovery mechanism (not currently implemented) would be needed to re-deliver it. Without that, the `recurring_scan_run` row will remain in `running` status.

### 10.15 Graceful shutdown (SIGINT / SIGTERM)

`ctx.Done()` fires. Each `localWorkerLoop` checks `ctx.Done()` at the top of its loop and returns. `workerGroup.Wait()` in `Start()` blocks until all goroutines exit. `Stop()` then closes the Postgres pool and Redis client within a 10-second timeout. In-flight DAG polls are cancelled through the context — the corresponding message is not XACK'd and the run remains in `running` status until the next worker instance picks it up (if PEL recovery is in place) or until a manual fix.
